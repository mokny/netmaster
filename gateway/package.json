{
  "name": "netmaster-nas-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Zentraler NAS-Gateway für NetMaster: mountet Freigaben per SSHFS/NFS und exportiert sie über Web-API, FTP und SFTP; provisioniert lokale Samba-Accounts.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "ftp-srv": "^4.6.3",
    "ssh2": "^1.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ssh2": "^1.15.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  }
}
